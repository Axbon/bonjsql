#BOOM


```
CODE
´´´

Inspired by other libs such as yesql, jasql, preql, sqlt
Supporting only dbAdapters that has named params and
providing promises support via Q so that you can combine
and run one or several queries.